# Clinipharma — Plano de Implementação (Auditoria 2026-04)

**Fonte:** `docs/audit-fine-tooth-comb-2026-04.md` (20 lentes, 187 sub-itens)
**Status:** Em execução — o agente é o executor, aprovador humano faz review de cada PR.
**Log de execução:** `docs/execution-log.md`
**Data:** 2026-04-17

> Plano organizado em **waves** (PR-por-wave) em vez de sprints. Cada wave é independentemente deployável, testada, documentada e reversível via feature flag. O **pentest externo foi explicitamente removido** deste ciclo; compensamos com scanners automatizados (CodeQL + Gitleaks + Trivy + npm audit) + E2E de segurança + DAST opcional.

---

## Modelo de execução

### Princípios

1. **1 wave = 1 PR mergeável.** Main sempre verde. Rollback = revert do commit.
2. **Migration antes do código.** SQL aplicado em staging → validado → prod → depois o código que consome é mergeado.
3. **Feature flag para tudo arriscado.** Kill-switch em 10 segundos se algo explodir.
4. **Dual-write / dual-read em schema sensível.** Troca de tipo (ex.: money → cents) acontece em 2 PRs: aditivo, depois cutover.
5. **Gate humano antes de aplicar migration em prod.** Agente prepara, humano aprova e mergeia.
6. **Cada wave atualiza `docs/execution-log.md`** com timestamps, commits, testes e evidências.

### Checklist "Done" de cada wave

- [ ] Migration (se houver) idempotente, commentada, com rollback documentado
- [ ] Código com teste unitário + teste de integração quando aplicável
- [ ] Cobertura não regride (threshold global mantido em `vitest.config.ts`)
- [ ] `npm run lint` + `npx tsc --noEmit` sem erros
- [ ] `npm run test` verde
- [ ] E2E smoke verde em staging após deploy
- [ ] `docs/execution-log.md` atualizado
- [ ] Runbook/documentação associada atualizada
- [ ] Feature flag inicializada em `OFF` quando aplicável

---

## Waves

### Fase 0 — Safety net

**Wave 0 — Fundações (em execução)**

| Item | Entrega                                                                                                               |
| ---- | --------------------------------------------------------------------------------------------------------------------- |
| 0.1  | Feature flags infra — `supabase/migrations/044_feature_flags.sql`, `lib/features/index.ts`, testes                    |
| 0.2  | `ci.yml:security-scan` (CodeQL + Gitleaks + Trivy + npm audit)                                                        |
| 0.3  | `CODEOWNERS` + `.github/dependabot.yml` + doc de branch protection                                                    |
| 0.4  | `.github/workflows/offsite-backup.yml` + `.github/workflows/restore-drill.yml` + `docs/disaster-recovery.md` revisado |
| 0.5  | `docs/implementation-plan.md` (este arquivo) + `docs/execution-log.md` inicializado                                   |

### Fase 1 — Observabilidade + perímetro de segurança

Cada wave abaixo é independente, mas a ordem maximiza ganho cumulativo.

| Wave | Entrega                                                                                                                                                                       | Pré-req |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| 1    | Logger com redação PII + correlação (request-id/trace-id/span-id)                                                                                                             | W0      |
| 2    | Webhook dedup (`webhook_events(idempotency_key)`) + `runCronGuarded` com `pg_try_advisory_xact_lock` + `cron_runs`                                                            | W0      |
| 3    | Audit append-only + hash chain + `verify_audit_chain` RPC + cron noturno                                                                                                      | W2      |
| 4    | RBAC granular: `permissions`/`role_permissions`/`user_permission_grants` + `lib/rbac/permissions.ts`, migração gradual de `requireRole` → `requirePermission` feature-flagged | W3      |
| 5    | CSRF double-submit + HMAC `timingSafeEqual` + open-redirect allowlist + E2E de ataque                                                                                         | W1      |
| 6    | Health 3 camadas (`/live`, `/ready`, `/deep`) + `lib/metrics.ts` + `lib/alerts.ts` (email + PagerDuty)                                                                        | W1      |
| 7    | SLO burn rate multi-window + OTEL completo (`instrumentation.ts`, `fetchExternal`) + admin SLO dashboard                                                                      | W6      |

### Fase 2 — Correção transacional + schema

**Sequência crítica — não pode ser paralelizada.**

| Wave | Entrega                                                                                          | Pré-req              |
| ---- | ------------------------------------------------------------------------------------------------ | -------------------- |
| 8    | Money cents **phase 1** — colunas aditivas `*_cents bigint` + dual-write em services             | W0                   |
| 9    | Money cents **phase 2** — backfill + switch reads para cents + property tests de invariantes     | W8 (≥7 dias estável) |
| 10   | RPC `create_order_atomic` — substitui fluxo N+1 em `services/orders.ts`                          | W9                   |
| 11   | RPCs `apply_coupon_atomic` + `confirm_payment_atomic` + `lock_version` em aggregates             | W10                  |
| 12   | Schema hardening remainder — enums nativos, `ON DELETE RESTRICT`, UUIDv7, `deleted_at` unificado | W11                  |
| 13   | Index hardening (`CREATE INDEX CONCURRENTLY`) — FK, partial, covering, BRIN                      | W12                  |
| 14   | Pool/timeouts por role (`statement_timeout`, `lock_timeout`, `idle_in_transaction`)              | W13                  |

### Fase 3 — Capacidade e qualidade

| Wave | Entrega                                                                                                                  |
| ---- | ------------------------------------------------------------------------------------------------------------------------ |
| 15   | Particionamento `audit_logs` por mês + autovacuum tuning + BRIN                                                          |
| 16   | N+1 elimination — `lib/db/cached-queries.ts` + embedded selects + `mv_pharmacy_daily_metrics`                            |
| 17   | `fraud_signals` + self-dealing + velocity-check                                                                          |
| 18   | A11y — axe-core E2E + correções de contraste/ARIA                                                                        |
| 19   | Email — `List-Unsubscribe-Post` + quiet hours + digest + sanitização templates                                           |
| 20   | Testing infra — vitest projects (`unit`/`integration`) + property (`fast-check`) + contract tests + mutation (`stryker`) |

### Fase 4 — Operação madura

| Wave | Entrega                                                                                                |
| ---- | ------------------------------------------------------------------------------------------------------ |
| 21   | Four-eyes workflow + impersonation controlada                                                          |
| 22   | Status page pública + synthetic monitor (Playwright GH Actions) + RUM                                  |
| 23   | Hexagonal para `orders` — `core/` + `adapters/` + `services/orders.ts` orquestrador fino               |
| 24   | API versioning `/v1/` + OpenAPI gerada de Zod + deprecation headers (RFC 8594)                         |
| 25   | 9 runbooks em `docs/runbooks/` + `docs/on-call.md` + `docs/chaos-engineering.md` + postmortem template |

---

## Dependências

```
W0 ── W1 ── W5 (perímetro)
  │     └─── W6 ── W7
  │
  ├── W2 ── W3 ── W4
  │               │
  ├── W8 ── W9 ── W10 ── W11 ── W12 ── W13 ── W14 ── W15
  │
  └── W16..W25 (podem iniciar em paralelo após respectivos pré-reqs)
```

---

## Numeração de migrations

Migrations existentes vão até `043_server_logs.sql`. As novas continuam a partir de `044`:

| Migration | Wave | Finalidade                                                        |
| --------- | ---- | ----------------------------------------------------------------- |
| 044       | W0   | Feature flags                                                     |
| 045       | W2   | `webhook_events` + `cron_runs`                                    |
| 046       | W3   | Audit hardening (hash chain)                                      |
| 047       | W4   | Fine-grained permissions                                          |
| 048       | W8   | Money cents — colunas aditivas                                    |
| 049       | W9   | Money cents — cutover (drop numeric)                              |
| 050       | W10  | `create_order_atomic`                                             |
| 051       | W11  | `apply_coupon_atomic` + `confirm_payment_atomic` + `lock_version` |
| 052       | W12  | Schema hardening (enums, FKs, UUIDv7, soft-delete)                |
| 053       | W13  | Index hardening                                                   |
| 054       | W14  | Pool & timeouts                                                   |
| 055       | W15  | `audit_logs` partitioning                                         |
| 056       | W16  | Materialized views                                                |
| 057       | W17  | `fraud_signals`                                                   |
| 058       | W21  | Four-eyes + impersonation                                         |
| 059       | W22  | Status page + RUM                                                 |

---

## Budget e governança

- **Pentest externo:** removido deste ciclo (decisão 2026-04-17). Substituído por scanners automáticos + E2E security.
- **PagerDuty:** starter plan a provisionar quando W6 estiver pronta (~$21/user/mês).
- **S3 offsite:** Cloudflare R2 (custo próximo a zero no volume atual) a configurar em W0.4.
- **Codecov / UptimeRobot:** free tier.

### Review por wave

- Agente abre PR, marca reviewer (humano aprovador).
- Humano revisa, aprova ou pede ajuste.
- Humano mergeia após aprovação (ou agente mergeia se configurado auto-merge).
- Agente monitora deploy em staging por tempo definido em `docs/execution-log.md`.
- Somente após estabilidade confirmada em staging, migration é aplicada em prod e código promovido.

---

_Para detalhes técnicos de cada correção ver `docs/audit-fine-tooth-comb-2026-04.md`._

_Wave 15 concluída (2026-04-17): rotação automática de secrets com modelo de 3 tiers e ledger hash-chain — migration 056 aplicada em prod criando `public.secret_rotations` (append-only via trigger `_secret_rotations_guard` bloqueando UPDATE/DELETE, hash chain SHA-256 estilo Wave 3/12/13/14, **`seq bigserial UNIQUE NOT NULL`** para ordering estritamente monotônico — descoberto durante o smoke do genesis: `now()` retorna o início da transação então 19 inserts no mesmo `DO $$` block tinham `rotated_at` idêntico, e `id` UUID v4 não tem ordem temporal, gerando "17 hash chain break(s) detected"; fix combinou `seq` + troca de `now()` para `clock_timestamp()` no DEFAULT + reordenar tudo por `seq DESC` no chain compute), RPC SECURITY DEFINER `secret_rotation_record(p_secret_name, p_tier, p_provider, p_trigger_reason, p_rotated_by, p_success, p_error_message, p_details)` validando tier ∈ {A,B,C} + provider whitelist + reason enum, view `public.secret_inventory` com último **sucesso** por secret + `age_seconds` + `age_days` + `last_row_hash`, RPC `secret_rotation_overdue(p_max_age_days)` com manifesto estático embutido de 19 secrets, genesis seed populando 19 baseline entries com `trigger_reason='genesis'`, smoke test fim-a-fim verificando 19 inventory rows / 0 overdue / 0 chain breaks (com `ORDER BY seq DESC`), flags `secrets.rotation_enforce` (severity warning → critical) e `secrets.auto_rotate_tier_a` (default OFF para bootstrap); **modelo de 3 tiers**: Tier A (3 — `CRON_SECRET`, `METRICS_SECRET`, `BACKUP_LEDGER_SECRET`) random bytes app-internal **rotacionados automaticamente** pelo cron via `vercel.rotateEnvValue(key, randomBytes(32).toString('base64url'))` + redeploy ÚNICO ao final da run mesmo com N rotações para evitar 60-120s × N de cold-start; Tier B (11 — Resend, Asaas API + webhook, Zenvia, Inngest event + signing, Clicksign access + webhook, Nuvem Fiscal, `VERCEL_TOKEN` com circularidade documentada §3.2.7, Turnstile) o cron prepara work-item com `rotation_strategy='tier_b_queued'` + runbook anchor + alerta warning para on-call (sem touch-Vercel — janela dual-key precisa de operador); Tier C (5 — `SUPABASE_DB_PASSWORD`, `SUPABASE_JWT_SECRET` com `invalidatesSessions=true,hasSiblings=true`, `FIREBASE_PRIVATE_KEY`, `OPENAI_API_KEY`, `ENCRYPTION_KEY` com `destroysDataAtRest=true` — Wave 6 PII at rest exige key versioning antes de rotacionar, documentado "NUNCA naively" no runbook §4.5) o cron só alerta com runbook link + janela de manutenção exigida; `lib/secrets/manifest.ts` isomorphic (sem `server-only` para CI scripts poderem importar) com `SECRET_MANIFEST` (19 `SecretDescriptor` tipados), `TIER_MAX_AGE_DAYS` (A/B 90d, C 180d), `manifestFingerprint()`; `lib/secrets/vercel.ts` server-only com cliente API minimalista — `listEnvs()` paginação até 5 páginas defensivas, `findEnv(key, target)`, `updateEnvValue(envId, newValue)` PATCH `/v9/projects/:projectId/env/:envId`, `rotateEnvValue(key, newValue)` lookup + patch atômico retornando envId + previous fingerprint, `triggerRedeploy(reason)` POST `/v13/deployments` com `gitSource={type:'github',ref:'main'}` + `target='production'` + meta tags `rotation-source=cron:rotate-secrets`, `fingerprint(value)` SHA-256 primeiros 8 hex (não loga secret nunca, mas permite "env vivo == ledger" verificação), erros tipados `VercelConfigError` (env Vercel ausente — `skipped-misconfigured`, falha de ops não de código) vs `VercelApiError` (non-2xx — `failed`, paga warning crítico) com labels distintas em métricas; `lib/secrets/rotate.ts` server-only orquestrador com `getOverdueSecrets()` (3 chamadas paralelas ao RPC dedup por name), `executeTierARotation`/`prepareTierBRotation`/`alertTierCRotation`, `rotateAllOverdue(opts)` com leitura única de flag (não 1× por secret) + dispatch por tier + redeploy único + falha de redeploy NÃO marca rotação como falha (env já está atualizado, falta só o cold-start) emite alerta crítico `secrets:redeploy-failed` apontando `vercel deploy --prod --force`, `getRotationStatus()` snapshot read-only para deep health (não dispara rotação fresh — custo), `recordManualRotation()` para o operador post-rotação manual de Tier B/C em maintenance window ou incident response (`reason='incident-confirmed-leak'`) retornando `row_hash` para o ticket; cron `/api/cron/rotate-secrets` (Node runtime, `vercel.json` `0 4 * * 0` = Sunday 01:00 BRT — menor atividade, weekly não daily para evitar fadiga de alerta), `withCronGuard` da W2, severity `critical` se `counts.failed > 0` OU (`requiresOperator > 0` E enforce ON) senão `warning`, operator list truncada em 30 entries para evitar payload bloat, catch de erro do orquestrador emite `dedupKey='secrets:cron:misconfigured'`; `app/api/health/deep/route.ts` ganha `checks.secretRotation` (`ok=false` quando enforce ON e há overdue/never-rotated, sem disparar rotação fresh); 8 métricas novas (`secret_rotation_runs_total{tier,outcome}`, `secret_rotation_failures_total{tier,secret,reason}`, `secret_rotation_overdue_count`, `secret_rotation_never_rotated_count`, `secret_age_seconds{secret}`, `secret_oldest_age_seconds`, `secret_rotation_duration_ms{tier,secret}` histogram, `secret_rotation_last_run_ts`); **`docs/slos.md`** adiciona **SLO-12 Secret freshness** (hard, security — Tier A/B ≤90d, Tier C ≤180d, never-rotated=0, owner Security+SRE, cadência semanal); **`docs/sli-queries.md`** 8 PromQL queries cobrindo SLO-12; `monitoring/grafana/security.json` +6 painéis (oldest age days, overdue, never-rotated, runs by tier+outcome, FAILURES, per-secret heatmap); runbook `docs/runbooks/secret-compromise.md` (P0/P1) cobrindo contexto do modelo de 3 tiers, sintomas de leak, triagem T+0 (snapshot do ledger + chain integrity check + Vercel env fingerprint match), rotação programada por tier (procedimento detalhado para cada um dos 11 Tier B + 5 Tier C — incluindo a circularidade de `VERCEL_TOKEN` que rotaciona a si mesmo com cron pausado, e `ENCRYPTION_KEY` que exige key versioning antes de qualquer rotação), incident response (suspected vs confirmed leak), comunicação DPO/Jurídico/CEO/ANPD (Art. 46 §1º), pós-mortem template, recovery de Tier A redeploy failure, verification pós-rotação (ledger hash chain + Vercel env fingerprint comparison); `docs/secrets-inventory.md` inventário humano dos 19 secrets + classificação tier + provider + queries SQL operacionais (daily check, weekly report, audit trail integrity) + mapeamento LGPD Art. 46 + ISO 27001 A.10.1.2; `lib/features/index.ts` adiciona `secrets.rotation_enforce` e `secrets.auto_rotate_tier_a` ao union type; `.env.example` documenta `VERCEL_TOKEN`/`VERCEL_PROJECT_ID`/`VERCEL_TEAM_ID` com nota explicando que sem eles o cron cai para alert-only mesmo em Tier A; **48 unit tests novos** (14 `lib/secrets-manifest` para drift check `manifest.ts` ⇄ `056.sql` parseando ambos arquivos + diff dos tuples `(name,tier,provider)` + invariantes (nomes únicos, tiers válidos, providers whitelist, descrições não-vazias, `ENCRYPTION_KEY` com `destroysDataAtRest=true`, `SUPABASE_JWT_SECRET` com `invalidatesSessions=true`); 14 `lib/secrets-rotate` para `getOverdueSecrets` agrega 3 tiers sem dup + happy path Tier A → Vercel PATCH + ledger record + ÚNICO redeploy mesmo com 2 secrets + failure paths (Vercel API erro vs `VercelConfigError` ⇒ `skipped-misconfigured`) + Tier B queued sem touch-Vercel + Tier C requires-operator com blast-radius flags + redeploy failure ⇒ alerta crítico mas rotação NÃO revertida + secret fora do manifest ⇒ defensive skip + `getRotationStatus` snapshot + gauges + never-rotated count = manifest_size − inventory_size; 8 `api/rotate-secrets-cron` para 0 overdue ⇒ no alert + warning para queued + requires (enforce OFF) + critical para failed > 0 (regardless enforce) + critical para Tier C requires (enforce ON) + orchestrator throws ⇒ `secrets:cron:misconfigured` + operator list truncated em 30 entries + redeploy reportado no body + 401 quando bearer ausente; 12 `lib/secrets-vercel` para env missing ⇒ `VercelConfigError` + teamId ausente é OK + `listEnvs` paginação + `findEnv` filter por target + `rotateEnvValue` happy path GET → PATCH + HTTP errors ⇒ `VercelApiError` com status/endpoint/body + `triggerRedeploy` POST `/v13/deployments` com gitSource main + `fingerprint` determinístico 8-char hex) elevando total para **1538 passing**; envs novos para deploy: `VERCEL_TOKEN` + `VERCEL_PROJECT_ID` + `VERCEL_TEAM_ID` (documentados em `.env.example`, manifest path `lib/secrets/manifest.ts:155-167` lista `VERCEL_TOKEN` como Tier B com circularidade documentada; cron cai para alert-only se ausentes, falha graceful com `skipped-misconfigured`)._

_Wave 14 concluída (2026-04-17): RLS canary — prova diária de isolamento de tenants — migration 055 aplicada em prod criando `public.rls_canary_log` (append-only via trigger `_rls_canary_log_guard` bloqueando UPDATE/DELETE, hash chain SHA-256 estilo Wave 3/12/13), RPC `rls_canary_assert(uuid)` **SECURITY INVOKER** (PostgreSQL proíbe `SET ROLE` dentro de SECURITY DEFINER — 42501) que itera matriz declarativa de **40 tabelas** classificadas em buckets `tenant`/`self`/`admin` e devolve `(table_name, bucket, visible_rows, expected_max=0, violated, error_message)` por linha — `permission denied` é tratado como enforcement válido (visible_rows=0, violated=false), RPC `rls_canary_record(...)` SECURITY DEFINER com `pg_advisory_xact_lock(hashtext('rls_canary'))` serializando writers + flag `rls_canary.page_on_violation` default OFF; **a primeira execução do smoke da migração descobriu DOIS bugs reais de produção**: (1) `clinic_members_select` tinha `EXISTS(SELECT 1 FROM clinic_members cm WHERE cm.clinic_id = clinic_members.clinic_id AND cm.user_id = auth.uid())` — referência recursiva à própria tabela, gerando `42P17 infinite recursion`. Não impactou prod apenas porque a app lê via service_role (BYPASSRLS) — qualquer rota nova com cliente authenticated quebraria; (2) ciclo `doctors_select` ↔ `doctor_clinic_links_select` via `EXISTS` cruzado. Ambos corrigidos na própria migração via helpers SECURITY DEFINER com `REVOKE PUBLIC + GRANT authenticated,service_role` (`is_clinic_member(clinic_id, user_id)`, `is_pharmacy_member(...)`, `is_doctor_for_user(doctor_id, user_id)`, `doctor_visible_to_clinic_member(doctor_id, user_id)`) que fazem bypass apenas do lookup mantendo semântica idêntica; `lib/rls-canary.ts` server-only com `signCanaryJwt(sub, ttl=60)` HS256 puro em `node:crypto` (sem nova dep — `jose` não justifica para 1 operação), `canarySubjectUuid()` (UUID fresh por run para impossibilitar mascaramento via seed), `runCanary()` orquestrando JWT-forge → cliente supabase-js com `Authorization: Bearer` (PostgREST resolve role=authenticated do bearer, RLS dispara) → `rls_canary_assert(subject)` → `rls_canary_record(...)` via service_role + métricas — fail-safe `violations=1` se RPC falhar (preferimos page falsa-positiva a canário cego), cap de 50 entries no `details.violating` para evitar bloat jsonb, `readLatestCanaryStatus()` para o deep health com gauge `rls_canary_age_seconds`; cron `/api/cron/rls-canary` (Node runtime, `vercel.json` `40 7 * * *` UTC = 04:40 BRT, `withCronGuard` da W2) — 0 violações ⇒ log info; ≥1 violação ⇒ `triggerAlert` severity controlada por flag (warning OFF / critical ON) com dedupKey `rls:canary:violation`; falha de start (env faltando) ⇒ alerta crítico próprio com dedupKey `rls-canary:misconfigured`; `app/api/health/deep/route.ts` ganha `checks.rlsCanary` lendo último run do ledger (`ok=false` se idade > 36h ou violations > 0, sem disparar canário fresh — custo); 7 métricas novas (`rls_canary_runs_total{outcome=ok|violation|error}`, `rls_canary_violations_total`, `rls_canary_tables_checked` gauge, `rls_canary_last_success_ts`, `rls_canary_last_violation_ts`, `rls_canary_age_seconds`, `rls_canary_duration_ms` histogram); **`docs/slos.md`** adiciona **SLO-11 RLS tenant isolation** (hard, security — qualquer `rls_canary_violations_total > 0` em janela 7d é budget exhausted + P0/P1 conforme flag) com owner Security+SRE e cadência diária; **`docs/sli-queries.md`** PromQL primária (`increase(rls_canary_violations_total[7d])`) + suplementares (runs por outcome, age gauge, duration p95, matriz size); `monitoring/grafana/security.json` +4 painéis (SLO-11 violations 7d com threshold red ≥1, age gauge yellow @25h red @36h, tables in matrix, runs by outcome timeseries); runbook `rls-violation.md` P0/P1 cobrindo triagem (ler ledger, classificar bucket, confrontar PRs recentes, reproduzir local), mitigação por cenário (policy quebrada / recursão infinita com pattern de helper SECURITY DEFINER / permission-denied que virou erro de schema), freeze de release com label `do-not-merge:rls`, comunicação DPO/Jurídico/CEO/ANPD (Art. 48 §6º), ligar/desligar enforcement; `docs/rls-matrix.md` matriz declarativa documentada (24 tabelas tenant + 5 self + 11 admin + 17 exclusões justificadas) com procedimento "como adicionar uma nova tabela"; `lib/features/index.ts` adiciona `rls_canary.page_on_violation` ao union type; `.env.example` documenta `SUPABASE_JWT_SECRET` (PostgREST jwt secret, fetched de `https://api.supabase.com/v1/projects/{ref}/postgrest`); **16 unit tests novos** (11 `lib/rls-canary.test.ts` exercitando JWT forging contra `node:crypto` real + `canarySubjectUuid` uniqueness + `runCanary` happy/violation/RPC-error/cap-50/persist-failure + `readLatestCanaryStatus` empty/age/error + 5 `api/rls-canary-cron.test.ts` para 0 violations / warning OFF / critical ON / runCanary throws / message truncation @20) elevando total para **1490 passing**; pendente para deploy: `vercel env add SUPABASE_JWT_SECRET production` (não temos Vercel token na automação, documentado no runbook §10)._

_Wave 13 concluída (2026-04-17): legal hold + investigações ANPD/CDC — migration 054 aplicada em prod criando `public.legal_holds` append-only (trigger `_legal_holds_guard` bloqueia DELETE + mutação de colunas imutáveis — subject_type/subject_id/reason/reason_code/document_refs/placed_at/placed_by/requestor — mantendo apenas o caminho `active → released | expired` válido), partial unique index `(subject_type, subject_id, reason_code) WHERE status='active'` prevenindo holds duplicados, RPCs SECURITY DEFINER `legal_hold_apply()` (idempotente: retorna row existente em unique_violation), `legal_hold_release()` (exige release_reason ≥10 chars), `legal_hold_is_active(subject_type, subject_id)` STABLE + GRANT authenticated (usada por RLS future-ready), `legal_hold_expire_stale()` (chamada do enforce-retention mensal — sem cron dedicado), view `legal_holds_active_view` filtrada por status + expires_at, **redefinição** de `audit_purge_retention()` com `RETURNS TABLE(purged_count, checkpoint_id, held_count)` (DROP+CREATE dentro da migração transacional pois CREATE OR REPLACE não aceita mudança de shape) pulando linhas cujo `actor_user_id` OR `entity_id::uuid` (cast defensivo via regex UUID para evitar `invalid input syntax`) está sob hold, flags `legal_hold.block_purge` e `legal_hold.block_dsar_erasure` default OFF; `lib/legal-hold.ts` server-only com zod v4 schemas (`applyHoldSchema`/`releaseHoldSchema`), `applyLegalHold()`/`releaseLegalHold()`/`isUnderLegalHold()` (com cache opcional `Map<subject, boolean>` para retention crons iterando múltiplas linhas — **fail-safe=HELD** em erro de RPC para nunca destruir evidência por erro transitório), `listActiveHolds`/`listAllHolds`, `expireStaleHolds`, `refreshActiveHoldGauge`, `recordPurgeBlocked(job, count)`; 3 endpoints admin DPO-only — `POST /api/admin/legal-hold/apply` (201 nova / 200 `idempotent:true` via detecção `placed_by !== actor.id`), `POST /api/admin/legal-hold/release`, `GET /api/admin/legal-hold/list?scope=active|all` — todos emitindo audit_logs tamper-evident (W3) com o hold_id como entityId; hard guard em `POST /api/admin/lgpd/anonymize/:userId`: quando sujeito sob hold e flag ON retorna 409 `LEGAL_HOLD_ACTIVE` **antes** de qualquer mutação, quando OFF emite `legal_hold_blocked_dsar_total{subject_type}` e segue (observe-only); `lib/retention-policy.ts` expandido para `RetentionSummary` com `profilesHeldByLegalHold`/`notificationsHeldByLegalHold`/`auditLogsHeldByLegalHold`/`legalHoldsExpired`, sweep de expiries + gauge refresh no início do cron, cache local por run, notifications agora list-then-filter-then-delete-by-id para possibilitar o skip em vez do antigo bulk delete; `app/api/health/deep/route.ts` ganha check `legalHolds` reportando `active_count` + `overdueExpiries`; 6 métricas novas (`legal_hold_apply_total{reason_code,outcome}`, `legal_hold_release_total{outcome}`, `legal_hold_active_count`, `legal_hold_blocked_purge_total{job}`, `legal_hold_blocked_dsar_total{subject_type}`, `legal_hold_expired_total`); **`docs/slos.md`** adiciona **SLO-10 Legal-hold preservation** (hard, legal — qualquer `legal_hold_blocked__*total > 0` com enforcement ON é budget exhausted + P1) com owner DPO+Legal e cadência event-driven; \**`docs/sli-queries.md`\*\* PromQL primária (increase 30d em blocked*_) + secundária (active*count, apply rate por reason_code, expired); `monitoring/grafana/money-and-dsar.json` +4 painéis (active holds, DSAR blocked 30d, purge rows held 30d, apply por reason); runbook `legal-hold-received.md` (P2 recepção / P1 suspeita de purge indevido) cobrindo triagem T+0 a T+1h (identificar subject_id via cpf_hash/numero_pedido/cnpj, curl do apply, confirmar no banco), verificação de "já deletamos algo?" (audit_logs por entity/actor + audit_chain_checkpoints + dsar_requests histórico + backup_latest_view para restore R2), estratégia de flags (recomendação de ligar `block_dsar_erasure` imediatamente + `block_purge` após 30d de observação do blocked_total), liberação, métricas, pós-incidente; **32 unit tests novos** (17 `lib/legal-hold` + 11 `api/legal-hold-routes` + 2 `api/lgpd` para paths 409/200 com flag ON/OFF + 2 `lib/retention-policy` para held_count e skip) levando total para **1474 passing**, 0 lint errors, 0 tsc errors; sem env vars novas.*

_Wave 12 concluída (2026-04-17): backup + restore drills de verdade — migration 053 aplicada em prod criando `public.backup_runs` (append-only via trigger `_backup_runs_append_only` bloqueando UPDATE/DELETE, hash-chained per `(kind,label)`) + RPC SECURITY DEFINER `backup_record_run(kind, label, r2_prefix, files_sha256, size_bytes, outcome, metadata, source_url)` com `pg_advisory_xact_lock(hashtext(kind||':'||label))` serializando writers concorrentes para preservar ordem da chain, RPC `backup_verify_chain(kind)` verificando linkage `prev_hash → row_hash` (não re-hash de conteúdo — jsonb text sort é pg-internal-dependent, mas append-only trigger + linkage é criptograficamente equivalente), view `public.backup_latest_view` e flag `backup.freshness_enforce` (default OFF durante bootstrap de 30d); `lib/backup.ts` server-only com `recordRunSchema` (zod v4 com `z.record(z.string(), z.unknown())` em metadata), `recordBackupRun` emitindo `backup_record_total{kind,outcome,result}` e gauges `backup_last_success_ts`/`backup_last_size_bytes`/`restore_drill_last_success_ts`, `getBackupFreshness` (com `age_seconds` computado via `now() - recorded_at`), `verifyBackupChain` desembrulhando o TABLE(...) envelope, e constantes `BACKUP_SLA` (9d weekly, 35d monthly); endpoint novo `POST /api/backups/record` (Node runtime, adicionado em `PUBLIC_ROUTES` do middleware) gated por `BACKUP_LEDGER_SECRET` via `Authorization: Bearer` ou `x-backup-ledger-secret` com `safeEqualString` time-safe — retorna 500 se secret ausente em prod-like (mesma política de `/api/metrics`), 401 em mismatch, 400 em JSON inválido, 422 problem+json em schema violation com `errors[{path,message}]`, 502 problem+json se RPC falhar (workflow retry 3x); cron novo `/api/cron/backup-freshness` (daily 09:00 UTC em `vercel.json`) lendo `backup_latest_view` + verificando chain por kind paralelo (`Promise.all`), emitindo gauges `backup_age_seconds{kind,label}`/`restore_drill_age_seconds{label}`, classificador puro `diagnoseFreshness(rows, chainBreaks)` retornando diagnoses com reason `missing|stale|last_failed|chain_break`, severity warning (flag OFF) ou critical (flag ON) com dedupKey `backup:freshness`; `.github/workflows/offsite-backup.yml` e `restore-drill.yml` receberam step "Record outcome to platform ledger" com retry exponencial (2s/5s/15s) que não falha o backup se o ledger estiver down — R2 é fonte de verdade; `app/api/health/deep/route.ts` incluiu `checks.backupFreshness` (`ok=false` só com flag ON, espelha comportamento do cron); **`docs/slos.md`** adiciona SLO-09 (“Backup + restore recoverability”) hard/DR com owner Platform+SRE; **`docs/sli-queries.md`** seção SLO-09 com PromQL para age check combinado, breach/chain counters, size regression, record rate; `monitoring/grafana/money-and-dsar.json` +4 painéis (backup age weekly, drill age monthly, chain breaks 30d, size regression); runbook `backup-missing.md` (P2↔P1) cobrindo 6 cenários (schedule pausado, R2 creds expiradas, AGE key lost, ledger 5xx, chain break, reset pós-recovery) + quick reference com `gh workflow run`; **35 unit tests novos** (15 `lib/backup` + 9 `api/backups/record` + 11 `api/cron/backup-freshness`) elevando total para **1442 passing**; new env `BACKUP_LEDGER_SECRET` (pendente rollout em Vercel + GH Actions secrets)._

_Wave 11 concluída (2026-04-17): observabilidade ponta-a-ponta — novo módulo `lib/trace.ts` server-only com parser/emitter W3C `traceparent` (rejeita all-zero), `fetchWithTrace()` drop-in wrapper injetando `traceparent` (span filho fresco) + `x-request-id` em todo outbound (+ counter `http_outbound_total{service,method,outcome}` + histogram `http_outbound_duration_ms{service,method,status}` + timeout AbortController default 10s + classificação `ok/error_4xx/error_5xx/error_network/error_timeout`), `enrichSentryScope()` usando Sentry v8 `getCurrentScope()` para stampar `request_id`/`trace_id`/`span_id`/`path`/`method`/`userId`; `middleware.ts` passa a honrar inbound `traceparent` W3C (ou mintar com UUID compactado quando ausente) e propaga tanto no request header (forward para Node) quanto no response header; `lib/logger/wrap.ts::withRouteContext` agora lê `traceparent` e popula `traceId`/`spanId` no AsyncLocalStorage (garantia: toda log line dentro de handler tem trace id); 5 novas métricas em `lib/metrics.ts` (`HTTP_OUTBOUND_TOTAL`, `HTTP_OUTBOUND_DURATION_MS`, `HTTP_REQUEST_DURATION_MS`, `HTTP_REQUEST_TOTAL`, `METRICS_SCRAPE_TOTAL`); novo endpoint `/api/metrics` (Node runtime, `force-dynamic`, added to middleware `PUBLIC_ROUTES`) protegido por `METRICS_SECRET` via `Authorization: Bearer` ou `?token=` com `safeEqualString` time-safe — retorna 500 quando secret ausente em prod-like (`VERCEL_ENV=production|preview`), 401 em mismatch, 200 `text/plain; version=0.0.4` default ou JSON via `?format=json`, aberto em dev com warn único; **`docs/slos.md`** publica 8 SLOs (checkout ≥99.5%, webhook idempotency 100% hard, auth p95 ≤400ms, cron freshness ≥99.9% hard, rate-limit FP ≤1%, DSAR SLA 0-breach hard/legal, money drift 0 hard/financial, 3rd-party ≥99%) com classificação hard/soft, error budgets 30d, ownership por SLO e política burn-rate multi-window (fast 14.4× / slow 6×); **`docs/sli-queries.md`** catálogo PromQL executável 1:1 com as queries; **`monitoring/grafana/*.json`** 3 dashboards JSON-as-code (22 painéis, sem plugins, compatível Grafana 10+) — `platform-health.json` (SLO-01/03/04/08), `security.json` (SLO-05 + CSRF + Turnstile + suspicious IPs + audit chain), `money-and-dsar.json` (SLO-02/06/07 + atomic RPC outcomes + DSAR pipeline + fallback usage) + `README.md` com instruções de import por `curl` e config de scrape em Vector; runbook `observability-gap.md` (P2) com triagem <5min, ground-truth checks (deploy, CF WAF, log volume, Sentry sampling, trace id correlation), mitigação em 4 cenários (500 em prod, dashboards vazios, ALS sem trace id, Sentry silente) e quick reference; **25 unit tests novos** (17 `lib/trace` + 8 `api/metrics-endpoint`) elevando total para **1407 passing**._

_Última atualização: 2026-04-19 — Wave 8 concluída (integer-cents em todo o caminho P&L: migration 050 aplicada em staging+prod com 14 colunas `*_cents BIGINT` espelhando `numeric(x,2)` em 7 tabelas (`orders`, `order_items`, `payments`, `commissions`, `transfers`, `consultant_commissions`, `consultant_transfers`), 7 triggers BEFORE sincronizando bidirecionalmente numeric ↔ cents com `RAISE EXCEPTION P0001` quando writer discorda dos dois, função IMMUTABLE `_money_to_cents(numeric)`, view `public.money_drift_view` listando linhas com drift > 1 cent, e flag `money.cents_read` (default OFF) gating dual-read; `lib/money.ts` com primitivos inteiros `toCents`/`fromCents`/`sumCents`/`mulCentsByQty`/`percentBpsCents`/`percentDecimalCents`/`driftCents`/`formatCents`/`readMoneyField` validados contra os pitfalls clássicos `0.1 + 0.2` e `2.36 * 100`; `lib/money-format.ts` server-only como adapter gated no flag com fail-closed; cron novo `/api/cron/money-reconcile` (every 30 min via `vercel.json`) emitindo counters `money_drift_total{table,field}` + alerta P2 deduplicado com runbook inline; RPCs atômicos (W7) continuam escrevendo apenas numeric e cents são preenchidos automaticamente via trigger — nenhuma mudança nos contratos W7; 60 unit tests novos (42 money + 13 format + 5 cron) levando o total para 1292 passing; runbook `money-drift.md` com árvore de decisão por padrão de drift, queries de diagnóstico e 3 mitigações)._

_Wave 10 concluída: migration 052 aplicada em staging+prod criando `public.rate_limit_violations` (minute-bucketed aggregation table, `ip_hash = sha256(ip||salt)` LGPD-safe, PK via unique index `(bucket, ip_hash, bucket_minute)`) + view `public.rate_limit_report_view` (last-hour rollup with top-offender ordering) + RPCs SECURITY DEFINER `rate_limit_record(text, text, uuid, jsonb)` (upsert com `hits=hits+1` em conflito) e `rate_limit_purge_old(int)` (30-day retention) + flag `security.turnstile_enforce` (default OFF); upgrade de `lib/rate-limit.ts` adicionando `guard(req, limiter, bucket)` com persistence via RPC (void — fire-and-forget), `X-RateLimit-*` + `Retry-After` headers, RFC 7807 problem+json body, fail-open em erro do limiter, counter `rate_limit_hits_total{bucket,outcome}` + histogram `rate_limit_check_duration_ms{bucket}` + counter `rate_limit_denied_total{bucket}`, `extractClientIp()` (XFF leftmost) e `hashIp()` (SHA-256 com `RATE_LIMIT_IP_SALT` + warn-once em missing), + 6 pre-configured limiters novos (`lgpdFormLimiter 3/h`, `lgpdExportLimiter 5/h`, além dos existentes `authLimiter`/`apiLimiter`/`registrationLimiter`/`exportLimiter`), + `Bucket` constants (`LGPD_DELETION`, `LGPD_EXPORT`, `AUTH_FORGOT`, `REGISTER_SUBMIT`, etc.); novo módulo `lib/turnstile.ts` com `verifyTurnstile({token, remoteIp, bucket, required})` (fail-open quando flag OFF, fail-closed quando flag ON + secret missing, 5s AbortController timeout, timeout-or-duplicate mapeado como `softFailure`, counters `turnstile_verify_total{bucket,outcome}` + histogram `turnstile_verify_duration_ms`) e `extractTurnstileToken(req)` suportando header `x-turnstile-token`, JSON `{turnstileToken|cf-turnstile-response}`, e form-data `cf-turnstile-response`; rotas `lgpd/deletion-request` (3/h per-user), `lgpd/export` (5/h per-user), `auth/forgot-password` (5/min per-IP) e `registration/submit` (3/10min per-IP) promovidas ao `guard()` + `verifyTurnstile` (role OFF durante rollout); novo cron `/api/cron/rate-limit-report` (every 15 min via `vercel.json`) com severity ladder P3 info / P2 warn (≥10 IPs OR >100 hits/IP) / P1 crit (≥50 IPs OR >500 hits/IP OR >5 buckets/IP credential-stuffing signal), dedup keys `rate-limit:spike:crit` e `rate-limit:spike:warn`, retention via `rate_limit_purge_old(30)` a cada run (best-effort), counter `rate_limit_suspicious_ips_total{severity}` e `classifyReport()` puro exportado para teste; 46 unit tests novos (16 `lib/rate-limit-guard` + 16 `lib/turnstile` + 14 `api/rate-limit-report`) levando total para 1382 passing; runbook `rate-limit-abuse.md` com tabela de padrões (single-IP vs many-IP, credential-stuffing vs form-spam), 3 mitigações (Cloudflare WAF block, Turnstile enforce, bucket budget lowering), queries de diagnóstico, ground-truth false-positive checks, escalation path para Security em 30 min em P1 com credential-stuffing signature._

_Wave 9 concluída: migration 051 aplicada em staging+prod, criando `public.dsar_requests` (queue com state-machine validated via trigger) + `public.dsar_audit` (append-only hash-chained) + `profiles.anonymized_at` / `anonymized_by` + RPCs SECURITY DEFINER `dsar_transition(uuid, text, jsonb)` e `dsar_expire_stale(int)`; `lib/dsar.ts` server-only com `createDsarRequest`/`transitionDsarRequest`/`hashCanonicalBundle`/`signCanonicalBundle`/`verifyCanonicalBundle` (HMAC-SHA256 sobre canonical JSON, timingSafeEqual na verificação); `lib/audit::logPiiView()` helper emitindo `action='VIEW_PII'` com scope; rotas `lgpd/deletion-request` e `lgpd/export` promovidas ao DSAR queue (export agora assinado com `X-LGPD-Export-Signature: sha256=<hex>`); `admin/lgpd/anonymize` agora popula `anonymized_at` + fecha a ERASURE DSAR com `delivery_hash`; cron novo `/api/cron/dsar-sla-check` (hourly, `0 * * * *`) com severity-ladder P1 (flag ON + breach) / P2 (breach sem flag OU warning-only), dedup keys `lgpd:dsar:sla:breach` e `lgpd:dsar:sla:warning`, auto-expire via `dsar_expire_stale(30)` só com `dsar.sla_enforce` = true; 8 métricas novas (`dsar_opened_total`, `dsar_transition_total`, `dsar_transition_error_total`, `dsar_transition_duration_ms`, `dsar_sla_breach_total`, `dsar_sla_warning_total`, `dsar_expired_total`, `dsar_duplicate_open_total`); 44 unit tests novos (31 `lib/dsar` + 8 `cron/dsar-sla-check` + 5 `logPiiView`) levando o total para 1336 passing; runbook `dsar-sla-missed.md` com árvore de decisão por tamanho de backlog, 4 estratégias de mitigação (fulfill manual / reject com código legal / kill-switch / recovery de erasure parcial), tabela de reject_codes (`NFSE_10Y`, `RDC_22_2014`, `ART_37_LGPD`) e playbook de escalação para ANPD conforme Art. 48._
