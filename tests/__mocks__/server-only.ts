// Stub for Next.js `server-only` package — not installed at test time.
// Importing this module in production throws if the file is bundled for the
// client. In tests we just want an empty module so imports succeed.
export {}
